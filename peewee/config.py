# install postgresql driver in MacOS by $pip install psycopg2-binary

DB = {
        'db': 'benchmark',
        'host': 'localhost',
        'port': 5432, # default port for postgresql
        'user': 'postgres',
        'password': 'zzy123456'
    }

